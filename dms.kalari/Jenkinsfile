pipeline {
    agent any
    stages {
        stage('Check Tools') {
            steps {
                sh 'which git'
                sh 'git --version'
                sh 'mvn --version'
            }
        }
    }
}

